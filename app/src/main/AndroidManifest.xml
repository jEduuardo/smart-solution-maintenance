<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartsolution.maintenance">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartSolution"
        android:usesCleartextTraffic="true">

        <!-- Splash / Entry Point -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartSolution.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".activities.LoginActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity android:name=".activities.RegisterActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Main -->
        <activity android:name=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Instituições -->
        <activity android:name=".activities.InstituicaoListActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity android:name=".activities.InstituicaoFormActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity android:name=".activities.SetorFormActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Equipamentos -->
        <activity android:name=".activities.EquipamentoListActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity android:name=".activities.EquipamentoFormActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Manutenção -->
        <activity android:name=".activities.ManutencaoListActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity android:name=".activities.ManutencaoFormActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Consumo -->
        <activity android:name=".activities.ConsumoListActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>
        <activity android:name=".activities.ConsumoFormActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Alertas -->
        <activity android:name=".activities.AlertasActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Atividades de Campo -->
        <activity android:name=".activities.AtividadeCampoFormActivity"
            android:screenOrientation="portrait"
            android:exported="false"/>

        <!-- Firebase Messaging -->
        <service
            android:name=".utils.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>nifest>