<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.SmartSolutionMaintenance">
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartSolutionMaintenance.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" android:exported="false"/>
        <activity android:name=".activities.MainActivity" android:exported="false"/>
        <activity android:name=".activities.DashboardActivity" android:exported="false"/>
        <activity android:name=".activities.ConsumoActivity" android:exported="false"/>
        <activity android:name=".activities.EquipamentosActivity" android:exported="false"/>
        <activity android:name=".activities.ManutencaoActivity" android:exported="false"/>
        <activity android:name=".activities.RegistroAtividadeActivity" android:exported="false"/>
        <activity android:name=".activities.InstituicaoActivity" android:exported="false"/>
        <activity android:name=".activities.NovoRegistroConsumoActivity" android:exported="false"/>
        <activity android:name=".activities.NovaManutencaoActivity" android:exported="false"/>
        <activity android:name=".activities.NovoEquipamentoActivity" android:exported="false"/>
        <service android:name=".utils.FCMService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>